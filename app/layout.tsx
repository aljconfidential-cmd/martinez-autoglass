import './globals.css';
import type { Metadata } from 'next';
import { Inter } from 'next/font/google';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'Martinez Autoglass - Professional Auto Glass Repair & Replacement',
  description: 'Expert windshield repair and replacement in Vermont. Same-day service, mobile repairs, all insurance accepted. Call Jaime at 602-875-4840.',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en" className="scroll-smooth">
      <body className={inter.className}>{children}</body>
    </html>
  );
}